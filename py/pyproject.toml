[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxgig-sdk-company-enrich"
version = "0.0.1"
description = "Unofficial generated Python SDK for the Company Enrich public API. Not affiliated with or endorsed by the upstream API provider."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
keywords = ["voxgig", "sdk", "generated-sdk", "openapi", "api-client", "company-enrich"]
dependencies = [
    "requests>=2.33",
]

[project.urls]
Homepage = "https://github.com/voxgig-sdk/company-enrich-sdk"
Repository = "https://github.com/voxgig-sdk/company-enrich-sdk"
Issues = "https://github.com/voxgig-sdk/company-enrich-sdk/issues"

# ONE package. core/entity/feature/utility were previously top-level packages;
# those names are real PyPI distributions and common scratch filenames, and
# Python searches the working directory first, so any of them beside the
# caller silently shadowed the SDK's own. They now live inside
# companyenrich_sdk/ where nothing can reach them.
[tool.setuptools.packages.find]
include = ["companyenrich_sdk*"]

# Ship the PEP 561 py.typed marker(s) so the inline type hints reach consumers.
[tool.setuptools.package-data]
"*" = ["py.typed"]
